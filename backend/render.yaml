services:
  - type: web
    name: dsa-tracker-api
    env: node
    region: singapore
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 24.19.0
      - key: MONGODB_URI
        sync: false
